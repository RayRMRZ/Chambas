// ignore_for_file: use_full_hex_values_for_flutter_colors

import 'package:flutter/material.dart';

//Colores globales para de acuerdo a la paleta
class Colores {
  static const azul = Color.fromRGBO(1,161,157, 1.0);
  static const rojo =  Color.fromRGBO(224,94,94, 1.0);
  static const amarillo = Color.fromRGBO(255,179,68, 1.0);
  static const crema = Color.fromRGBO(255,248,228, 1.0);

  const Colores();
}